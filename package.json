{
  "name": "clovapi",
  "private": true,
  "version": "0.0.0",
  "dependencies": {
    "ssh2": "^1.17.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:api": "go run ./backend",
    "dev:web": "npm --prefix web run dev",
    "deploy": "node scripts/deploy.mjs"
  }
}
