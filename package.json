{
  "name": "clovapi",
  "private": true,
  "version": "0.0.0",
  "dependencies": {
    "ssh2": "^1.17.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "npm --prefix web run dev",
    "dev:platform": "npm --prefix platform run dev",
    "build": "node scripts/build.mjs",
    "build:web": "npm --prefix web run build",
    "build:landing:cloudflare": "npm --prefix landing run build",
    "check:platform": "npm --prefix platform run typecheck && npm --prefix platform test",
    "core:watch": "node scripts/watch-core.mjs",
    "deploy:legacy-vps": "node scripts/deploy.mjs",
    "validate:cloudflare": "node scripts/validate-cloudflare-release.mjs",
    "check:web": "npm --prefix web run check"
  }
}
