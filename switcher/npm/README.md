# @clovapi/cli

Install `clovapi` as a global npm command:

```bash
npm i -g @clovapi/cli
clovapi version
```

The package downloads platform binaries from GitHub Releases and verifies SHA256 checksums.

## Environment override

- `CLOVAPI_CLI_BASE_URL`: override download base URL for mirrors or local testing.
